# Claude Çalışma Notları — MERT.4

## Aktif Branch
`claude/debug-system-pages-M8P0c`

## Tamamlanan İşler
1. `autoSeedIfEmpty()` — pouchdb.ts'e eklendi (localStorage→PouchDB→CouchDB)
2. `GlobalTableSyncContext` — 15 hardcoded bileşen → `TABLE_SYNC_CONFIGS` dizisi (DRY)
3. `restartAllSync()` + `window.online` listener — ağ kurtarma
4. `db-config.ts` — runtime URL deteksiyonu (`window.location.origin + '/couchdb'`)
5. `pouchdb.ts startSync()` — auth header custom fetch ile her zaman gönderiliyor
6. `docker-compose.yml` — `VITE_COUCHDB_URL=""` (build-time hardcode kaldırıldı)
7. `guncelleme_notlari` tablosu — db-config, pouchdb, storage, updateNotes, GlobalTableSyncContext
8. `updateNotes.ts` — SEED_NOTES + seedUpdateNotesToDb() + CRUD fonksiyonları

## Devam Eden İşler
- [ ] `updater.py` tam yeniden yazma (tab UI + matplotlib + sistem tepsisi)
  - Orijinal: 2726 satır, tek uzun sayfa
  - Hedef: ttk.Notebook (6 sekme), matplotlib grafikler, pystray sistem tepsisi
  - Backend metodlar korunacak, sadece UI değişecek
  - Kritik widget isimleri: self.console, self.progress, self.elapsed_lbl,
    self.status_dot, self.status_label, self.health_dot, self.health_lbl,
    self.couch_dot, self.couch_lbl, self.commit_label, self.behind_lbl,
    self.commits_box, self._backup_inner, self._backup_canvas,
    self.backup_docker_var, self._couch_stats_inner, self._couch_stats_frame,
    self._cfg_db_url_var, self._cfg_user_var, self._cfg_pass_var,
    self._cfg_status_lbl, self.auto_var, self._timer_toggle_btn,
    self._timer_interval_var, self._timer_countdown_lbl,
    self._summary_content, self._summary_menu, self._summary_ids,
    self._chart_cpu, self._chart_ram, self._chart_couch, self._chart_couch_line,
    self._chart_health, self._chart_build, self._chart_code,
    self._timeline_canvas, self._ENV_FILE

- [ ] `StokPage.tsx` senkronizasyon hatası düzeltilecek

## Son Commit
ddf7265 — guncelleme_notlari tablosu altyapısı
